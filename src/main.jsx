import React from 'react';
import ReactDOM from 'react-dom/client';
import HardNotes from './HardNotes.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <HardNotes />
  </React.StrictMode>
);